# Copy to .env.local and fill in values for local development.

# ── Site login wall (pre-launch) ─────────────────────────────────────────────
# Set SITE_AUTH_ENABLED=false to browse locally without logging in.
SITE_AUTH_ENABLED=true
SITE_AUTH_USERNAME=najibwebsite
# Quote values if they contain # or other special characters.
SITE_AUTH_PASSWORD=
SITE_AUTH_SESSION_TOKEN=

# ── Robotic surgery page (/robotic-surgery) ──────────────────────────────────
# YouTube video ID only (the part after watch?v=). Optional — the page uses
# the self-hosted MP4 at /video/da-vinci-5-promotional.mp4 when this is empty.
NEXT_PUBLIC_ROBOTIC_SURGERY_YOUTUBE_ID=
